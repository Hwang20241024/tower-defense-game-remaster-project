import { config } from '../../config/config.js';

class Monster {
  constructor(monsterId, monsterNumber) {
    this.id = monsterId;
    this.number = monsterNumber;
    this.x = x ?? 0;
    this.y = y ?? 0;
  }
}

export default Monster;
